[package]
name = "tentaflow-zvec"
version = "0.1.0"
edition = "2021"
description = "Safe RAII wrapper over the zvec embedded vector database C API."
license = "Apache-2.0"

[dependencies]
tentaflow-zvec-sys = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
