[engine]
id = "claude-code"
category = "agents"
resource_kind = "ai"
provider = "external"
name = "Claude Code"
description_pl = "Oficjalny Claude Code CLI z natywnym logowaniem, trwałymi sesjami i dostępem do workspace."
description_en = "Official Claude Code CLI with native login, durable sessions, and workspace access."
homepage = "https://docs.anthropic.com/en/docs/claude-code"
license = "Proprietary"
icon = "claude"
default_port = 8765
api = "custom"
# Required by the manifest schema. A managed-cli engine installs whatever the
# vendor's release channel reports as newest, so this is a label, not a pin.
version = "latest"
requires_model = false
gpu_supported = false
service_surfaces = ["chat"]
input_modalities = ["text"]
output_modalities = ["text"]

[deploy.native]
platforms = ["linux", "macos"]
runtime = "managed-cli"
binary_path = "agents/native/coding-agent-bridge"
