// =============================================================================
// Plik: roles/mod.rs
// Opis: Rejestr modulow per-rola. Kazda rola ma `pub async fn run(cfg) -> Result<()>`.
// =============================================================================

pub mod reverse_proxy;
