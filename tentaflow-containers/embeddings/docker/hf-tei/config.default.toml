service_name = "embeddings"
model_aliases = ["embedding-gemma", "default"]

[quic]
port = 5000

[role]
kind = "reverse_proxy"
upstream_url = "http://127.0.0.1:8087"
timeout_ms = 60000
api = "open_ai"
