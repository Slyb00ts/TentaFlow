# Manifest serwisu Jina Embeddings v5 (MLX) — wbudowany silnik embeddingow dla
# Apple Silicon (macOS/iOS). Runtime embedded przez MLX (feature
# inference-mlx, ta sama sciezka co nemotron-embed-mlx); brak
