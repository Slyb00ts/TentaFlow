{
  "blocks": [
    {
      "type": "uppercase",
      "category": "Text",
      "label": "Showcase: Uppercase",
      "description": "Test flow block — takes text, returns it uppercased. Validates the full invoke_block path (fresh instance + per-call fuel + JSON envelope).",
      "icon": "shield",
      "inputs": [
        { "name": "in", "type": "text" }
      ],
      "outputs": [
        { "name": "full", "type": "text" }
      ],
      "config_schema": {}
    }
  ]
}
