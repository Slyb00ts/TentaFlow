{
  "blocks": [
    {
      "type": "uppercase",
      "category": "Text",
      "label": "Test: Uppercase",
      "description": "Test flow block — przyjmuje text, zwraca uppercase. Waliduje cale invoke_block path (fresh instance + per-call fuel + JSON envelope).",
      "icon": "shield",
      "inputs": [
        { "name": "in", "type": "text" }
      ],
      "outputs": [
        { "name": "full", "type": "text" }
      ],
      "config_schema": {}
    }
  ]
}
