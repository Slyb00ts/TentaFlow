[package]
name = "tentaflow-addon-embeddings-chunker"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
tentaflow-addon-sdk = { path = "../../addon-sdk/sdk" }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
